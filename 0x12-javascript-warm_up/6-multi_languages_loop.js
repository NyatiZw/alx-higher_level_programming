#!/usr/bin/node

const a = 'C is fun';
const b = 'Python is cool';
const c = 'JavaScript is amazing';
const lang = [`${a}`, `${b}`, `${c}`];

for (let i = 0; i < lang.length; i++) {
  console.log(lang[i]);
}
